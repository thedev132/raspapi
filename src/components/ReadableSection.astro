---
interface Props {
    title: string;
}

const { 
    title
} = Astro.props;
---

<div class="bg-gray-900 text-green-400 p-4 rounded-lg mb-4 border-2 border-green-800 transform transition-transform duration-300 hover:scale-105">
    <div class="flex items-center space-x-2">
        <span class="text-green-500">➜</span>
        <span class="text-sm uppercase tracking-wider">{title}</span>
    </div>
    
    <div class="bg-black p-3 rounded mt-2">
        <div class="mt-2 ml-2 text-green-400 text-lg text-wrap overflow-x-auto prose prose-invert">
            <slot />
        </div>
    </div>
</div>